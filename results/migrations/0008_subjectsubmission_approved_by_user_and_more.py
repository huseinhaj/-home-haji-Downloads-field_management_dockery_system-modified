# Generated by Django 5.2.8 on 2026-07-01 15:05
#
# NOTE: originally added submitted_by_user/approved_by_user as FKs to
# field_app.CustomUser plus a TeacherProfile model. That design was replaced
# (2026-07-02) by results.TeacherAccount, a self-contained account model that
# lives in the results app's own database and never references CustomUser
# (cross-database foreign keys aren't supported). Left as a no-op so the
# already-applied state on the old 'default' database stays consistent;
# 0009 creates the real fields/model fresh.

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('results', '0007_school_schoolsubject_exam_school'),
    ]

    operations = []
